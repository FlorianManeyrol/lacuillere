<% @restaurants.each do |restaurant| %>
  <h2><%= restaurant.name %></h2>
  <p><%= restaurant.address %></p>
  <%= link_to 'Show me details', restaurant_path(restaurant)%>
<% end %>

<%= link_to 'Create a restaurant', new_restaurant_path %>
